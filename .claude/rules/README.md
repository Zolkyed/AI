# Rules

Add a `.md` file here to define a rule scoped by file glob rather than directory. It is loaded only when a matching file is touched, regardless of where in the repository that file lives. This complements nested `AGENTS.md` files, which are directory-scoped instead.
