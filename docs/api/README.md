# API

This directory contains the API contract, usage guidance, and runnable manual request examples.

## Contents

- [`requests/`](requests/README.md) — manual `.http` requests grouped by API area
- `openapi.yaml` — the API contract, when one is introduced

Automated API tests belong in `test/api/` or `test/integration/`, not in this directory.
